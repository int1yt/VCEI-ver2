\documentclass[tikz,border=10pt]{standalone}
\usepackage{ctex}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}

\begin{document}
\begin{tikzpicture}[
  font=\small,
  >=Latex,
  cell/.style={draw=black!50, minimum width=6mm, minimum height=4.8mm},
  mark/.style={draw=red!80, very thick, rounded corners=1mm}
]

% long timeline strip
\foreach \i in {0,...,23} {
  \node[cell, fill=blue!7] (a\i) at (\i*0.7, 0) {};
}
\node[above] at (4.0,0.55) {CAN 报文序列（按时间排序）};

% 64-window indication (schematic)
\draw[mark] (a3.south west) rectangle (a14.north east);
\node[above, color=red!80] at (5.95,1.2) {窗口 \(W=64\)（示意缩放）};
\draw[->, thick] (a3.north) -- ++(0,0.6);

% matrix
\node[draw=black!70, fill=green!8, rounded corners=2mm, minimum width=32mm, minimum height=22mm, align=center] (mat) at (7.8,-2.0) {
输入矩阵 \(\mathbf{X}\in\mathbb{R}^{64\times9}\)\\
列 1--8: Data0..Data7\\
列 9: \(\Delta t\)（ms）归一化
};

\draw[->, very thick] (a9.south) -- node[right] {滑窗+特征拼接} (mat.north);

\node[draw=black!35, rounded corners=2mm, fill=orange!12, align=left, inner sep=4pt] at (2.6,-2.0) {
步长 \(S=32\)\\
样本重叠增强\\
时序连续性
};

\end{tikzpicture}
\end{document}
